# handoffs/

One note per open lane (`<branch-slug>.md`), deleted when the lane merges.
This is the only channel between the Claude and Codex sessions — they never
share context. See `docs/agents/parallel-work.md`.

Template:

    # <branch>
    Agent: claude|codex
    Head: <sha>

    ## What changed
    ## Why
    ## Verified
    ## Assumed / left out
    ## Review focus
