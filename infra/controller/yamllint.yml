---
extends: default

rules:
  comments:
    min-spaces-from-content: 1
  document-start: disable
  line-length:
    max: 240
    level: error
    ignore: |
      infra/secrets/*.sops.yml
      infra/secrets/**/*.sops.yml
  truthy:
    allowed-values: ["true", "false"]
    check-keys: false
